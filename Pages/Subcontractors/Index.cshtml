@page
@model ConstructionManagementSystem.Pages.Subcontractors.IndexModel
@{
    ViewData["Title"] = "Taşeronlar";
    var canManage = BuildTaskFlowPermissions.CanManageSubcontractors(User);
}

<div class="page-heading">
    <div>
        <h1 class="h3 mb-1">Taşeronlar</h1>
        <p class="text-muted mb-0">Demirci, kalıpçı, elektrikçi ve tesisatçı taşeron firmalar.</p>
    </div>
    @if (canManage)
    {
        <a class="btn btn-primary" asp-page="Create">Yeni Taşeron Ekle</a>
    }
</div>

<div class="content-card p-3">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Firma</th>
                    <th>İş Türü</th>
                    <th>Yetkili</th>
                    <th>Telefon</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Subcontractors)
                {
                    <tr>
                        <td>@item.CompanyName</td>
                        <td>@item.WorkType</td>
                        <td>@item.ContactName</td>
                        <td>@item.Phone</td>
                        <td>
                            <div class="actions">
                                @if (canManage)
                                {
                                    <a class="btn btn-outline-primary btn-sm" asp-page="Edit" asp-route-id="@item.Id">Düzenle</a>
                                    <a class="btn btn-outline-danger btn-sm" asp-page="Delete" asp-route-id="@item.Id">Sil</a>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

