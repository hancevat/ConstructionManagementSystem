@page "{id:int}"
@model ConstructionManagementSystem.Pages.Suppliers.DeleteModel
@{
    ViewData["Title"] = "Tedarikçi Sil";
}

<div class="page-heading">
    <h1 class="h3 mb-0">Tedarikçi Sil</h1>
    <a class="btn btn-outline-secondary" asp-page="Index">Listeye Dön</a>
</div>

<div class="content-card form-card p-4">
    <div class="alert alert-warning">
        <strong>@Model.Supplier.CompanyName</strong> tedarikçi kaydını silmek istediğinizden emin misiniz?
    </div>
    <form method="post">
        <input type="hidden" asp-for="Supplier.Id" />
        <button type="submit" class="btn btn-danger">Sil</button>
        <a class="btn btn-outline-secondary" asp-page="Index">Vazgeç</a>
    </form>
</div>

