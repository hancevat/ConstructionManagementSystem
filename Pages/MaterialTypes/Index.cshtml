@page
@model ConstructionManagementSystem.Pages.MaterialTypes.IndexModel
@{
    ViewData["Title"] = "Malzeme Türleri";
    var canManage = BuildTaskFlowPermissions.CanManageMaterials(User);
}

<div class="page-heading">
    <div>
        <h1 class="h3 mb-1">Malzeme Türleri</h1>
        <p class="text-muted mb-0">Beton, demir, tuğla ve benzeri malzeme grupları.</p>
    </div>
    @if (canManage)
    {
        <a class="btn btn-primary" asp-page="Create">Yeni Malzeme Türü Ekle</a>
    }
</div>

<div class="content-card p-3">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Tür Adı</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.MaterialTypes)
                {
                    <tr>
                        <td>@item.Name</td>
                        <td>
                            <div class="actions">
                                @if (canManage)
                                {
                                    <a class="btn btn-outline-primary btn-sm" asp-page="Edit" asp-route-id="@item.Id">Düzenle</a>
                                    <a class="btn btn-outline-danger btn-sm" asp-page="Delete" asp-route-id="@item.Id">Sil</a>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

