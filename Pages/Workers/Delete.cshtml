@page "{id:int}"
@model ConstructionManagementSystem.Pages.Workers.DeleteModel
@{
    ViewData["Title"] = "İşçi Sil";
}

<div class="page-heading">
    <h1 class="h3 mb-0">İşçi Sil</h1>
    <a class="btn btn-outline-secondary" asp-page="Index">Listeye Dön</a>
</div>

<div class="content-card form-card p-4">
    <div class="alert alert-warning">
        <strong>@Model.Worker.FullName</strong> işçi kaydını silmek istediğinizden emin misiniz?
    </div>
    <form method="post">
        <input type="hidden" asp-for="Worker.Id" />
        <button type="submit" class="btn btn-danger">Sil</button>
        <a class="btn btn-outline-secondary" asp-page="Index">Vazgeç</a>
    </form>
</div>

