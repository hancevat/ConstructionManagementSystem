@page
@model ConstructionManagementSystem.Pages.ProjectIncomes.IndexModel
@{
    ViewData["Title"] = "Proje Gelirleri";
    var canManage = BuildTaskFlowPermissions.CanManageFinancialRecords(User);
}

<div class="page-heading">
    <div>
        <h1 class="h3 mb-1">Proje Gelirleri</h1>
        <p class="text-muted mb-0">Hakediş ve proje gelir kayıtları.</p>
    </div>
    @if (canManage)
    {
        <a class="btn btn-primary" asp-page="Create">Yeni Proje Geliri</a>
    }
</div>

<div class="content-card p-3">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Tarih</th>
                    <th>Proje</th>
                    <th>Tutar</th>
                    <th>Açıklama</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.ProjectIncomes)
                {
                    <tr>
                        <td>@item.IncomeDate.ToString("dd.MM.yyyy")</td>
                        <td>@item.ConstructionProject?.Name</td>
                        <td>@item.Amount.ToString("C")</td>
                        <td>@item.Description</td>
                        <td>
                            <div class="actions">
                                <a class="btn btn-outline-info btn-sm" asp-page="Details" asp-route-id="@item.Id">Detay</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

